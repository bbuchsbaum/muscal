# Plot subject clouds in RV and ROI spaces

Plot subject clouds in RV and ROI spaces

## Usage

``` r
plot_subjects(x, dim = c(1, 2))
```

## Arguments

- x:

  A fitted \`covstatis\` object.

- dim:

  A numeric vector of length 2 specifying the dimensions to plot.
