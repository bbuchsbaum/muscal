% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cv_muscal.R
\name{cv_muscal}
\alias{cv_muscal}
\title{Cross-Validate Muscal Models with Multidesign Folds}
\usage{
cv_muscal(
  folds,
  fit_fn,
  estimate_fn,
  truth_fn,
  task = NULL,
  metrics = NULL,
  performance_args = list(),
  ...
)
}
\arguments{
\item{folds}{A `foldlist` object, typically created by
`multidesign::fold_over()` or `multidesign::cv_rows()`.}

\item{fit_fn}{Function with signature `fit_fn(analysis)` returning a fitted
`muscal` model.}

\item{estimate_fn}{Function that produces held-out estimates from
`estimate_fn(model, assessment, ...)`.}

\item{truth_fn}{Function that extracts the truth object from
`truth_fn(assessment, ...)`.}

\item{task}{Optional task override. If `NULL`, the task is taken from
`model$task`.}

\item{metrics}{Optional metric vector passed to [performance_metrics()].}

\item{performance_args}{Optional named list of additional arguments forwarded
to [performance_metrics()] on every fold.}

\item{...}{Reserved for future extensions.}
}
\value{
An object inheriting from `cv_result` with additional fields `folds`,
  `foldframe`, `task`, `metrics`, and `call`.
}
\description{
A thin cross-validation wrapper that reuses `multidesign` fold objects and
the `muscal` fit contract. Users provide:
- `fit_fn(analysis)` to fit a model on the analysis split,
- `estimate_fn(model, assessment, ...)` to produce predictions or held-out
  estimates, and
- `truth_fn(assessment, ...)` to extract the corresponding truth object.
}
\details{
The returned metrics are computed through [performance_metrics()], so fold-wise
outputs have a stable, task-aware tibble shape that works with
`multidesign::cross_validate()`.

This function deliberately does not define a new split abstraction. Use
`multidesign::fold_over()` or `multidesign::cv_rows()` to construct `folds`.
}
