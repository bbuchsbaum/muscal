% Generated by hand for the current implementation slice.
\name{predict.response_aligned_mfa}
\alias{predict.response_aligned_mfa}
\title{Predict from a Response-Aligned MFA Fit}
\usage{
\method{predict}{response_aligned_mfa}(
  object,
  new_data,
  block,
  type = c("response", "scores", "reconstruction"),
  new_response = NULL,
  response_weight = NULL,
  new_anchor_map = NULL,
  anchor_weight = NULL,
  conditional = FALSE,
  preprocess = TRUE,
  ...
)
}
\arguments{
\item{object}{A fitted `response_aligned_mfa` object.}

\item{new_data}{Numeric matrix/data.frame of new rows from a known predictor
block.}

\item{block}{Character or integer identifying the block.}

\item{type}{One of `"response"`, `"scores"`, or `"reconstruction"`.}

\item{new_response}{Optional observed multivariate response rows used to refine
the latent-score solve when `conditional = TRUE`.}

\item{response_weight}{Optional scalar or vector of non-negative row weights
paired with `new_response`.}

\item{new_anchor_map}{Optional anchor assignments for the new rows used to
refine the latent-score solve.}

\item{anchor_weight}{Optional scalar or vector of non-negative row weights
paired with `new_anchor_map`.}

\item{conditional}{Logical; if `TRUE`, allow conditional completion with
`new_response`. The default `FALSE` keeps prediction target-pure.}

\item{preprocess}{Logical; if `TRUE` (default), applies the fitted predictor
and response preprocessing pipelines before projection.}

\item{...}{Unused.}
}
\value{
A numeric matrix of predicted responses, latent scores, or reconstructed block
rows.
}
\description{
Predict from a Response-Aligned MFA Fit
}
