% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/infer_muscal.R
\name{infer_muscal}
\alias{infer_muscal}
\title{Standard Resampling and Inference for Muscal Fits}
\usage{
infer_muscal(
  object,
  method = c("bootstrap", "permutation"),
  statistic = c("sdev", "loadings"),
  statistic_fn = NULL,
  nrep = 100,
  alpha = 0.05,
  seed = NULL,
  alternative = c("greater", "less", "two.sided"),
  refit = NULL,
  verbose = FALSE,
  ...
)
}
\arguments{
\item{object}{A fitted `muscal` model.}

\item{method}{One of `"bootstrap"` or `"permutation"`.}

\item{statistic}{One of `"sdev"` or `"loadings"`. Ignored if
`statistic_fn` is supplied.}

\item{statistic_fn}{Optional extractor `function(fit)`, used for custom
statistics. For permutation inference this must return a numeric vector.}

\item{nrep}{Number of resampling replicates.}

\item{alpha}{Tail probability used for bootstrap intervals and permutation
reference quantiles.}

\item{seed}{Optional RNG seed for reproducible resampling.}

\item{alternative}{Alternative hypothesis for permutation p-values.}

\item{refit}{Optional refit specification overriding `object$fit_spec$refit`.}

\item{verbose}{Logical; if `TRUE`, emit progress and failure counts.}

\item{...}{Reserved for future extensions.}
}
\value{
A list inheriting from `muscal_inference_result` and either
  `muscal_bootstrap_result` or `muscal_permutation_result`.
}
\description{
Runs bootstrap or permutation inference for `muscal` fits that expose a
standard refit contract through `fit_spec$refit`. This keeps resampling logic
generic across method families while still allowing each method to define its
own data reconstruction and null-generation strategy.
}
\details{
The default statistics currently supported are:
- `"sdev"`: component-wise standard deviations / singular values
- `"loadings"`: the concatenated loading matrix `fit$v` (bootstrap only)

Methods without stored refit metadata will error unless an explicit `refit`
specification is supplied.
}
