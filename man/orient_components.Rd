% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/orient_components.R
\name{orient_components}
\alias{orient_components}
\alias{orient_components.mfa}
\alias{orient_components.aligned_mfa}
\alias{orient_components.linked_mfa}
\alias{orient_components.aligned_interbattery}
\alias{orient_components.mcca}
\alias{orient_components.aligned_mcca}
\alias{orient_components.response_aligned_mfa}
\alias{orient_components.aligned_rrr}
\alias{orient_components.ipca}
\alias{orient_components.penalized_mfa}
\alias{orient_components.default}
\title{Orient Component Signs for Fitted muscal Models}
\usage{
orient_components(
  x,
  basis = NULL,
  reference = NULL,
  signs = NULL,
  rule = c("max_abs", "sum"),
  tol = 1e-08,
  ...
)

\method{orient_components}{mfa}(
  x,
  basis = NULL,
  reference = NULL,
  signs = NULL,
  rule = c("max_abs", "sum"),
  tol = 1e-08,
  ...
)

\method{orient_components}{aligned_mfa}(
  x,
  basis = NULL,
  reference = NULL,
  signs = NULL,
  rule = c("max_abs", "sum"),
  tol = 1e-08,
  ...
)

\method{orient_components}{linked_mfa}(
  x,
  basis = NULL,
  reference = NULL,
  signs = NULL,
  rule = c("max_abs", "sum"),
  tol = 1e-08,
  ...
)

\method{orient_components}{aligned_interbattery}(
  x,
  basis = NULL,
  reference = NULL,
  signs = NULL,
  rule = c("max_abs", "sum"),
  tol = 1e-08,
  ...
)

\method{orient_components}{mcca}(
  x,
  basis = NULL,
  reference = NULL,
  signs = NULL,
  rule = c("max_abs", "sum"),
  tol = 1e-08,
  ...
)

\method{orient_components}{aligned_mcca}(
  x,
  basis = NULL,
  reference = NULL,
  signs = NULL,
  rule = c("max_abs", "sum"),
  tol = 1e-08,
  ...
)

\method{orient_components}{response_aligned_mfa}(
  x,
  basis = NULL,
  reference = NULL,
  signs = NULL,
  rule = c("max_abs", "sum"),
  tol = 1e-08,
  ...
)

\method{orient_components}{aligned_rrr}(
  x,
  basis = NULL,
  reference = NULL,
  signs = NULL,
  rule = c("max_abs", "sum"),
  tol = 1e-08,
  ...
)

\method{orient_components}{ipca}(
  x,
  basis = NULL,
  reference = NULL,
  signs = NULL,
  rule = c("max_abs", "sum"),
  tol = 1e-08,
  ...
)

\method{orient_components}{penalized_mfa}(
  x,
  basis = NULL,
  reference = NULL,
  signs = NULL,
  rule = c("max_abs", "sum"),
  tol = 1e-08,
  ...
)

\method{orient_components}{default}(
  x,
  basis = NULL,
  reference = NULL,
  signs = NULL,
  rule = c("max_abs", "sum"),
  tol = 1e-08,
  ...
)
}
\arguments{
\item{x}{A fitted model object.}

\item{basis}{Basis used to choose the component signs. May be `NULL` (use a
class-specific default), a character scalar naming a stored field in `x`,
a numeric matrix/data frame, or a list of matrices that will be row-bound
before computing the sign rule.}

\item{reference}{Optional reference used to align signs. May be another
fitted model, a numeric matrix/data frame, or a list of matrices. When
supplied, signs are chosen to maximize per-component agreement between the
resolved basis for `x` and the resolved basis for `reference`.}

\item{signs}{Optional numeric vector of length equal to the number of
components. Non-zero values are coerced to `-1` or `+1`. When supplied,
`basis` and `reference` are ignored.}

\item{rule}{Rule used when `reference = NULL` and `signs = NULL`. `"max_abs"`
makes the largest-magnitude entry in each basis column positive. `"sum"`
makes the column sum positive.}

\item{tol}{Non-negative tolerance used when a sign decision is numerically
ambiguous.}

\item{...}{Unused; reserved for future extensions.}
}
\value{
An oriented copy of `x`.
}
\description{
Post-process a fitted model by applying a shared per-component sign choice
across scores, loadings, and cached derived quantities. This does not refit
the model. Reconstructions remain unchanged under the sign flip; projected
scores follow the new orientation.
}
\details{
Supported first-wave classes are `mfa`, `aligned_mfa`, `anchored_mfa` /
`linked_mfa` (including graph-anchored variants), `mcca`, `aligned_mcca` /
`anchored_mcca`, `response_aligned_mfa`, `aligned_rrr`,
`aligned_interbattery`, `ipca`, and `penalized_mfa`.

The sign convention can be supplied explicitly via `signs`, inferred from a
basis matrix or list of matrices via `basis`, or aligned to a reference fit
or matrix via `reference`.
}
