% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aligned_rrr_multifer.R
\name{infer_aligned_rrr}
\alias{infer_aligned_rrr}
\title{Multifer Inference for Aligned RRR}
\usage{
infer_aligned_rrr(
  x,
  data = NULL,
  targets = "default",
  B = 1000L,
  R = 500L,
  alpha = 0.05,
  seed = NULL,
  strict = TRUE,
  ...
)
}
\arguments{
\item{x}{A fitted `aligned_rrr` object.}

\item{data}{Optional original data list with elements `X` and `Y`. When
`NULL`, the data stored in `x$fit_spec$refit$data` are used.}

\item{targets}{Inference targets passed to [multifer::infer()]. Defaults to
all targets declared by the adapter: component significance plus loading,
score, and subspace stability.}

\item{B}{Number of Monte Carlo draws per component-significance rung.}

\item{R}{Number of bootstrap replicates for stability targets.}

\item{alpha}{Significance level passed to [multifer::infer()].}

\item{seed}{Optional random seed.}

\item{strict}{Logical; passed to [multifer::infer()].}

\item{...}{Additional arguments passed to [multifer::infer()].}
}
\value{
A `multifer::infer_result` object, or a `multifer` bundle when
  `return_bundle = TRUE` is passed through `...`.
}
\description{
Run `multifer` inference for a fitted [aligned_rrr()] model. The adapter uses
multifer's opaque predictive geometry so the model can keep its native
multiblock predictor/response data shape.
}
