% Generated by hand for the current implementation slice.
\name{project.response_aligned_mfa}
\alias{project.response_aligned_mfa}
\title{Project New Rows into a Response-Aligned MFA Space}
\usage{
\method{project}{response_aligned_mfa}(
  x,
  new_data,
  block,
  new_response = NULL,
  response_weight = NULL,
  new_anchor_map = NULL,
  anchor_weight = NULL,
  conditional = FALSE,
  preprocess = TRUE,
  ...
)
}
\arguments{
\item{x}{A fitted `response_aligned_mfa` object.}

\item{new_data}{Numeric matrix/data.frame of new rows for a known predictor
block.}

\item{block}{Character or integer identifying the block used for projection.}

\item{new_response}{Optional observed multivariate response rows used to refine
the latent-score solve when `conditional = TRUE`.}

\item{response_weight}{Optional scalar or vector of non-negative row weights
paired with `new_response`.}

\item{new_anchor_map}{Optional anchor assignments for the new rows. May be an
integer vector of anchor-state ids (with `NA` for unanchored rows) or a
non-negative row-stochastic matrix.}

\item{anchor_weight}{Optional scalar or vector of non-negative row weights
paired with `new_anchor_map`.}

\item{conditional}{Logical; if `TRUE`, allow conditional completion with
`new_response`. The default `FALSE` keeps projection pure with respect to the
response target.}

\item{preprocess}{Logical; if `TRUE` (default), applies the fitted predictor
and response preprocessing pipelines before projection.}

\item{...}{Unused.}
}
\value{
A numeric matrix of latent scores.
}
\description{
Project New Rows into a Response-Aligned MFA Space
}
