# Build a cluster graph from spatial coordinates

Build a cluster graph from spatial coordinates

## Usage

``` r
cluster_graph(coords_list, adjacency_opts = list())
```

## Arguments

- coords_list:

  List of coordinate matrices (each k_s x 3)

- adjacency_opts:

  Options for adjacency construction

## Value

Sparse adjacency matrix
