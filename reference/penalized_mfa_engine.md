# Core engine for penalized MFA optimization

Core engine for penalized MFA optimization

## Usage

``` r
penalized_mfa_engine(
  Xp,
  ncomp,
  lambda,
  penalty_method,
  max_iter,
  nsteps_inner,
  learning_rate,
  optimizer,
  beta1,
  beta2,
  adam_epsilon,
  tol_obj,
  tol_inner,
  verbose,
  compute_consensus
)
```
