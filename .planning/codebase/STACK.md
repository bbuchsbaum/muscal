# Technology Stack

**Analysis Date:** 2026-01-22

## Languages

**Primary:**
- R 3.5.0+ - Core statistical analysis package with multivariate methods

**Secondary:**
- C++ (C++11) - High-performance numerical computations using RcppArmadillo
- Roxygen2 documentation markup

## Runtime

**Environment:**
- R (version 3.5.0 or higher required)

**Build System:**
- R package build system with Roxygen2 for documentation generation

## Frameworks

**Core:**
- Rcpp 1.0+ - R/C++ interface for performance-critical code in `src/ridge_solve.cpp`
- RcppArmadillo - Armadillo linear algebra library bindings for C++ operations
- RcppEigen - Eigen library bindings for matrix operations (linked via LinkingTo)

**Analysis:**
- multivarious - Base multivariate analysis framework providing projectors and utilities
- multidesign - Multiblock design handling and utilities
- genpca - Generalized PCA implementation used for MFA algorithm

**Data Manipulation:**
- dplyr - Data manipulation using pipe operator `%>%`, select, tibble, bind_rows
- purrr - Functional programming utilities (map, map2)
- tibble - Modern data frame implementation
- abind - Array binding and manipulation

**Parallel Processing:**
- furrr - Futures-based parallel mapping (used in bootstrap operations)

**Validation & Utilities:**
- chk - Argument checking and validation (type checks, non-empty checks)
- assertthat - Assertion framework for runtime checks
- rlang - Advanced metaprogramming utilities (enquo, quo expressions)
- crayon - Terminal color output for messages
- cli - CLI formatting utilities (alerts, headings)
- MASS - Modern Applied Statistics with S (ginv - generalized inverse)
- Matrix - Sparse and dense matrix operations
- pracma - Practical numerical math (orth function)
- RSpectra - Sparse eigenvalue/SVD computations (svds)
- RANN - Approximate nearest neighbors

**Supporting:**
- stats - Base R statistics functions
- graphics - Plotting utilities
- utils - Base utilities (combn, head, tail, modifyList)

## Key Dependencies

**Critical:**
- `Rcpp` + `RcppArmadillo` - Ridge regression solver in C++ at `src/ridge_solve.cpp`
- `multivarious` - Provides core projection infrastructure and utilities used throughout
- `genpca` - Implements the underlying Generalized PCA algorithm for MFA computation
- `furrr` - Parallel bootstrap resampling in `bootstrap.bada()` at `R/bada.R:93`

**Matrix Computation:**
- `Matrix` - Sparse matrix support and efficient operations (Diagonal, crossprod, rankMatrix)
- `RSpectra` - Iterative SVD for large matrices (svds method)
- `MASS` - Generalized matrix inverse (ginv)

**Linear Algebra:**
- `RcppEigen` - Eigen C++ library integration for matrix algebra
- `pracma` - Orthonormalization (orth function)

**Data Analysis:**
- `MatrixCorrelation` - RV and RV2 similarity metrics for block correlation at `R/mfa.R:53, 58`

## Configuration

**Environment:**
- Roxygen2 documentation (version 7.3.3) - generates NAMESPACE and .R files
- No environment variables required for functionality
- No external configuration files needed (.env files not used)

**Build:**
- `DESCRIPTION` file defines package metadata, dependencies, version (0.1.0)
- `NAMESPACE` auto-generated by Roxygen2 (do not edit manually)
- C++ compilation via Rcpp::plugins(cpp11) at `src/ridge_solve.cpp:1`

## Platform Requirements

**Development:**
- R >= 3.5.0
- C++11 compiler for building C++ extensions
- Rcpp development tools

**Production:**
- R >= 3.5.0
- Pre-compiled binaries for platform (Windows, macOS, Linux)

---

*Stack analysis: 2026-01-22*
